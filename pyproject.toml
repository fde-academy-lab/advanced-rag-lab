[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "raglab"
version = "1.0.0"
description = "Advanced RAG & Evals Lab — a from-scratch retrieval stack and the evaluation that judges it, running entirely in memory"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "FDE LAB" }]
keywords = ["rag", "retrieval", "evaluation", "bm25", "reranking", "bedrock", "teaching"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Education",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = ["numpy>=1.24", "matplotlib>=3.7", "pandas>=2.0"]

[project.optional-dependencies]
bedrock = ["boto3>=1.34"]
neural  = ["sentence-transformers>=2.6"]
claude  = ["anthropic>=0.40"]
dev     = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.6", "nbclient>=0.9", "nbformat>=5.10",
           "jupyterlab>=4.2"]

[project.urls]
Homepage      = "https://github.com/fde-academy-lab/advanced-rag-lab"
Documentation = "https://github.com/fde-academy-lab/advanced-rag-lab/tree/main/docs"
Discussions   = "https://github.com/fde-academy-lab/advanced-rag-lab/discussions"
Issues        = "https://github.com/fde-academy-lab/advanced-rag-lab/issues"

[tool.setuptools.packages.find]
include = ["raglab*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
  "slow: takes more than a few seconds (deselect with '-m \"not slow\"')",
  "notebooks: executes notebooks end to end",
]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["notebooks"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4"]
ignore = [
  "E741",   # ambiguous variable name — `l` for a label reads fine in short comprehensions
  "B008",   # function call in default argument — not a pattern we use
  "B905",   # zip() without strict= — we zip equal-length sequences by construction and the
            # explicit strict=False everywhere adds noise without catching anything here
  "B007",   # unused loop variable — tuple unpacking for readability is deliberate
  "C416",   # "unnecessary" comprehension — sometimes the explicit form documents intent
]

[tool.ruff.lint.per-file-ignores]
# Seeded GitHub content is prose in string literals; wrapping it to 100 columns would
# reflow markdown that is meant to be read on GitHub.
"scripts/seed_content.py" = ["E501"]
# Seed modules are prose held in Python. Reflowing a discussion post to satisfy a
# line-length rule makes the diff unreadable and the content worse.
"scripts/seed/*.py" = ["E501"]
# CSS and HTML held in Python. Reflowing a stylesheet to satisfy a line-length
# rule makes it unreadable and changes nothing about the output.
"scripts/build_site.py" = ["E501"]
# Tests name things for legibility, not brevity.
"tests/*" = ["E501"]

[tool.coverage.run]
source = ["raglab"]
omit = ["raglab/bedrock.py"]      # network path, exercised only in integration
