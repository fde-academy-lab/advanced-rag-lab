# Paste into ~/.claude/CLAUDE.md (personal memory, every project)

## FDE Academy

- Cohort repositories are built from `fde-academy-lab/advanced-rag-lab` with the
  `cohort-repo-setup` skill. Read its `references/traps.md` before any GitHub API call.
- Never hallucinate a GitHub feature. Discussion categories, polls, project views, and the
  wiki's first page have no API. Say so and hand over the URL.
- Every number in prose needs a command that regenerates it.
- Green is not done. Read the surface back through the API and count.
- Dry-run before every mutation. Idempotent or nothing.
- Learners are tracked, never ranked. No public per-learner scores.
- Tokens never appear in chat or files. If one has, say once that it should be rotated.
- A permission refusal is a stop, not a detour.
- Give every browser step as a numbered list with the exact URL.
- Write cohort-facing prose without em dashes.
