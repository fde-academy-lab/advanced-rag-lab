# Every number correct, and it presents a 0.0036 difference as a result. "A small lead but
# consistent" is what a difference inside the noise band sounds like when no interval was
# computed, and it is how complexity gets shipped.
fails:
  - quotes an interval, not only means
